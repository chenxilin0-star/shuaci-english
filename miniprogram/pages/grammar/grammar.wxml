<view class="container"><view class="title">语法学习</view><view class="tabs"><text wx:for="{{categories}}" wx:key="*this" class="tag" bindtap="select" data-cat="{{item}}">{{item}}</text></view><view wx:for="{{filtered}}" wx:key="id" class="card"><view class="row"><view class="h2">{{item.title}}</view><text class="tag">{{item.frequency}}</text></view><view class="muted">{{item.category}}</view><view>{{item.summary}}</view><view class="grammar-content">{{item.content}}</view></view></view>