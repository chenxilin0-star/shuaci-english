# CloudBase Seed

在云开发控制台创建集合：`users`, `word_banks`, `words`, `grammar_topics`, `user_progress`, `favorites`, `checkins`, `mistake_books`。

可先将 `miniprogram/data/mockData.js` 中的示例数据导入 `word_banks`、`words`、`grammar_topics`，后续再使用 ECDICT 清洗后的完整 CET 数据替换。
